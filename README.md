# 🎯 SparkLearn: Ignite Your Learning

A complete, local-first AI-powered placement preparation platform. Runs fully on CPU.

---

##  Features

| Feature | Description |
|---|---|
| 🎤 **AI Interview Simulator** | Voice-based interview with real-time face & eye tracking proctoring |
| 👁️ **Face/Eye Proctoring** | OpenCV detects if you look away — 5+ warnings terminate the session |
| 🗣️ **Speech-to-Text** | Browser Web Speech API for recording answers |
| 🤖 **AI Answer Evaluation** | Gemini AI scores your answers on content, communication & depth |
| 🧠 **AI Quiz Generator** | Fresh MCQs generated by AI every session across 8 CS topics |
| 💻 **Coding Lab** | 6 curated problems (Easy→Hard) with AI hints, review & test execution |
| 📊 **Performance Dashboard** | Track study time, quiz scores, problems solved, interview sessions |
| 🏢 **Company Prep Guides** | AI-generated prep guide for any company (Google, TCS, Infosys, etc.) |
| 📄 **Resume Analyzer** | AI scores your resume, finds missing keywords, gives action items |
| 📝 **Mock Test** | Simulated full placement exam flow |
| 📚 **Study Material** | 8 CS topics (DBMS, SQL, DSA, OOP, CN, OS, Aptitude, Coding) |

---

## 📋 Requirements

- Python 3.8+
- Webcam + Microphone (for interview mode)
- Internet connection (for AI features)
- Google Gemini API key (free at [aistudio.google.com](https://aistudio.google.com))

**Works on:** Windows · macOS · Linux · CPU only (no GPU needed)

---

## ⚡ Quick Start

### 1. Clone / Extract the project
```bash
cd placement_system
```

### 2. Set up API key
```bash
cp .env.example .env
# Edit .env and add your GEMINI_API_KEY
```

### 3. Install dependencies
```bash
pip install -r requirements.txt
```

### 4. Run
```bash
python run.py
# OR
python app.py
```

### 5. Open browser
```
http://localhost:5000
```

---

## 🗂️ Project Structure

```
placement_system/
├ app.py                    # Main Flask application
├ run.py                    # Quick-start script
├ requirements.txt
├ .env.example
├ content/                  # Study material text files
│   ├ dbms.txt
│   ├ sql.txt
│   ├ dsa.txt
│   ├ oops.txt
│   ├ cn.txt
│   ├ os.txt
│   ├ aptitude.txt
│   └ coding_questions.txt
└ frontend/
    ├ templates/
    │   ├ base.html         # Shared layout + dark design system
    │   ├ setup.html        # Onboarding page
    │   ├ dashboard.html    # Main dashboard
    │   ├ interview.html    # AI Interview + proctoring
    │   ├ quiz.html         # AI Quiz generator
    │   ├ coding_editor.html# Coding lab + editor
    │   ├ study.html        # Study material viewer
    │   ├ performance.html  # Analytics dashboard
    │   ├ resume.html       # Resume analyzer
    │   ├ companies.html    # Company prep guides
    │   └ mock_test.html    # Mock test hub
    └ static/               # CSS, JS, assets
```

---

## 🔌 API Endpoints

| Method | Endpoint | Description |
|---|---|---|
| POST | `/api/ask_doubt` | AI answer to topic question |
| POST | `/api/generate_quiz` | Generate fresh MCQs |
| POST | `/api/get_question` | Get interview question |
| POST | `/api/evaluate_answer` | AI score interview answer |
| POST | `/api/detect_face` | OpenCV face/eye detection |
| POST | `/api/run_code` | Execute/simulate code |
| POST | `/api/get_code_hint` | AI coding hint |
| POST | `/api/review_code` | Full AI code review |
| POST | `/api/analyze_resume` | Resume scoring |
| POST | `/api/company_prep` | Company prep guide |
| POST | `/api/get_ai_feedback` | Overall readiness analysis |

---

## 🏗️ Architecture

```
Browser (HTML/CSS/JS)
     │
     ▼
Flask App (app.py)
     │
     ├ OpenCV (face/eye detection)  ← CPU only, local
     ├ Web Speech API               ← Browser-native STT
     └ Google Gemini API            ← AI features (cloud)
          ├ Quiz generation
          ├ Answer evaluation
          ├ Code review/hints
          ├ Resume analysis
          └ Company prep guides
```

---

## 🔑 Getting a Free Gemini API Key

1. Go to [https://aistudio.google.com/app/apikey](https://aistudio.google.com/app/apikey)
2. Sign in with your Google account
3. Click "Create API Key"
4. Copy the key to your `.env` file:
   ```
   GEMINI_API_KEY=AIza...
   ```

The free tier is sufficient for all features in this system.

---

##  Troubleshooting

**Camera not working?**
- Allow camera/microphone permissions in browser
- Use Chrome or Edge for best Web Speech API support

**AI features not working?**
- Check your `GEMINI_API_KEY` in `.env`
- Ensure internet connection

**OpenCV error?**
- Run: `pip install opencv-python-headless`

**Port already in use?**
- Change `PORT=5001` in `.env`

---

## 📈 Roadmap / Future Features

- [ ] User authentication & persistent database
- [ ] Faster Whisper (local STT, no internet needed)
- [ ] Local LLM support (Ollama/LlamaCpp)
- [ ] Resume PDF upload
- [ ] Leaderboard & peer comparison
- [ ] Company-wise question bank
- [ ] Email progress reports
- [ ] Mobile app (PWA)

---

## 📄 License

MIT License — Free for personal and educational use.

---

Built with ❤️ for placement preparation
